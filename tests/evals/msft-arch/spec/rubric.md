Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Tech-Design-First Approach** — Derives requirements from architecture design, not the other way around. Produces the architecture first (component types, technology choices, integration patterns) and then derives implementation tasks. Does not jump directly to implementation tasks without first establishing the design. Score 5 if architecture-first approach is clear with design driving task derivation; 1 if tasks are listed without architectural design rationale.

2. **C4 Diagram Completeness** — Produces all required C4 diagrams: System Context (always), Container (always), Component (for complex containers), Dynamic/Sequence (always for primary flow). Additionally produces Mermaid process flow diagrams: component interaction sequence, business process flowchart, state machine (for entities with lifecycle), error handling flow. Every arrow is labeled with action AND data. Score 5 if all required diagram types are present with labeled arrows and error paths; 1 if diagrams are missing or arrows are unlabeled.

3. **ADR Generation** — Emits an ADR file for every architectural decision: technology choices, integration patterns, identity models, deployment topology. At least two alternatives considered per ADR. Does not generate ADRs for configuration values or naming conventions. ADRs are cross-linked from design.md section 2.6. Score 5 if ADRs are complete with alternatives and cross-linked; 1 if no ADRs are produced or alternatives are missing.

4. **Functional Programming and DDD Business Logic** — Business logic is specified using functional programming patterns: pure functions with input/output signatures, Result types for error handling (not exceptions), no side effects in core logic. Data models follow DDD: value objects, aggregates, bounded contexts identified. Score 5 if functional paradigm and DDD are correctly applied to business logic and data models; 1 if OOP exception-based patterns dominate.

5. **Mid-Level Developer Readability** — The spec is complete enough for a mid-level developer to build the feature without further clarification: all API endpoints defined with request/response schemas and error responses, implementation tasks have clear dependencies and acceptance criteria, test strategy covers unit/integration/E2E. Score 5 if spec is complete and self-contained for implementation; 1 if critical implementation details are vague or missing.
