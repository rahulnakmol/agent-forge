Score the output 1-5 on each criterion. Return the AVERAGE.

1. **SLI/SLO/SLA Derivation Order** — Defines SLIs first (what to measure and how), then sets SLO targets from measured baseline data (not invented), and derives SLA commitments below the SLO with a documented cushion. Never invents SLOs from a committed SLA. Produces the SLO register with service name, SLI type, KQL measurement query, target %, rolling window, and error budget minutes. Score 5 if derivation order is correct with complete SLO register; 1 if SLOs are invented from SLAs or the register lacks measurement queries.

2. **Error Budget Governance Tiers** — Applies the correct governance policy tiers: >50% remaining = normal velocity; 25-50% = reliability caution; 10-25% = reliability focus; <10% = feature freeze; exhausted = full stop with escalation. Stops feature work when error budget is critically low rather than treating budgets as advisory. Score 5 if governance tiers are correctly applied and feature freeze is enforced below 10%; 1 if error budget exhaustion does not trigger feature work stoppage.

3. **Multi-Window Multi-Burn-Rate Alerting** — Configures two alert windows per SLO: fast-burn (14x multiplier, 1-hour window, critical severity, pages on-call) and slow-burn (2-5x multiplier, 6-hour window, warning severity, creates ticket). Provides KQL alert formula or references Azure Monitor SLI feature. Does not use simple threshold alerts. Score 5 if both fast-burn and slow-burn windows are configured with correct multipliers; 1 if threshold-based alerting is recommended without burn-rate windows.

4. **Chaos Engineering with Safety Controls** — Designs chaos experiments with: documented steady-state hypothesis, abort condition (Azure Monitor alert or metric threshold), low-traffic window scheduling, and SLO metric monitoring during runs. Uses Azure Chaos Studio fault taxonomy. Recommends production chaos with safety controls rather than staging-only. Specifies IaC delivery via Terraform/AzAPI. Score 5 if experiments have hypothesis, abort conditions, and IaC delivery; 1 if chaos is staging-only or lacks safety controls.

5. **Toil Measurement and 50% Governance Trigger** — Tracks toil as a percentage of SRE time per sprint. Enforces governance: when toil exceeds 50%, creates a toil-reduction epic and freezes new toil-generating manual processes. Applies Polly resilience patterns (retry, circuit breaker, bulkhead) for toil reduction. Does not accept toil >50% as a normal state. Score 5 if toil tracking, 50% governance trigger, and automation patterns are all present; 1 if toil is not measured or the 50% threshold is not enforced.
