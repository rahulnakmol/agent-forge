Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Correct Reference Path Identification** — Identifies the correct reference file paths from the standards library for the queried domain. Uses the exact relative paths from the standards index (e.g., `references/coding-stack/preferred-stack.md`, `references/security/stride-a-worksheet.md`). Does not invent paths or guess filenames. Score 5 if all referenced paths match the canonical standards index; 1 if paths are invented or incorrect.

2. **Skill-to-Reference Mapping Accuracy** — Correctly maps which skills consume which reference files: all-skills references (preferred-stack, review-checklist), security-specific references (stride-a-worksheet, identity-decision-tree), diagram references (c4-diagram-guide, lld-process-diagrams), domain-specific references (ef-core-checklist for dotnet-architect, finops-framework for finops-architect). Score 5 if skill-to-reference mapping is accurate and complete; 1 if mappings are incorrect or significant references are omitted.

3. **No-Duplication Principle** — Consistently states that no skill should duplicate content from the standards library; skills reference files by path rather than inlining content. Updates to standards propagate to all skills automatically. Does not reproduce standards content inline when directing users to references. Score 5 if no-duplication principle is clearly articulated and applied; 1 if content is duplicated or the reference-by-path pattern is not used.

4. **Reference Categorization** — Correctly categorizes references by their scope: universal (preferred-stack, functional-programming, DDD), security (stride-a-worksheet, identity-decision-tree, security-checklist), quality (review-checklist, quality-standards, accessibility-wcag), diagrams (c4-diagram-guide, mermaid-patterns, lld-process-diagrams), domain-specific (ef-core-checklist, finops-framework). Score 5 if categorization matches the canonical index structure; 1 if references are miscategorized or categories are ignored.

5. **Standards-as-Reference-Library Behavior** — The skill behaves as a reference library, not a design tool. It answers questions about which references to load, not about how to design systems. Correctly directs users to specialist skills for design decisions. Does not trigger on direct user invocations for design work. Score 5 if library behavior is maintained throughout (directing to references and specialists); 1 if the skill generates architecture advice instead of reference routing.
