Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Process Decomposition Discipline** — Decomposes business processes from L1 (Value Chain) through L4 (Activity) following the methodology. Each process includes: owner, frequency, SLA, input/output, and systems used. Does not jump to technology mapping before completing the process taxonomy. Does not skip the Discover phase — always gathers business problem, industry, org size, and transformation drivers before design. Score 5 if process decomposition is complete L1-L4 with all required attributes; 1 if processes are mapped to technology without decomposition or Discover phase is skipped.

2. **Maturity Framework Application** — Uses the 5-level maturity scale (1-Initial, 2-Developing, 3-Defined, 4-Managed, 5-Optimizing) consistently for current and target state assessment. Identifies maturity gaps and derives transformation priorities from those gaps. Does not design target state without assessing current state baseline. Score 5 if maturity scale is correctly applied with gap analysis driving priorities; 1 if target state is designed without current state assessment or maturity levels are not defined.

3. **AI Augmentation Overlay** — Every modern TOM includes AI classification for each L2 process using the 5 categories: Autonomous (no human), Human-in-the-Loop (AI executes, human approves), Copilot Assist (human with AI help), RPA (rule-based), Human Only (judgment required). Does not defer AI augmentation to "phase 2." Maps classifications to Microsoft Copilot or Azure OpenAI capabilities. Score 5 if all L2 processes are classified with AI category and Microsoft service mapping; 1 if AI augmentation is omitted or deferred.

4. **Microsoft Technology Mapping** — Maps TOM capabilities to the correct Microsoft technology layer: D365 F&O/CE for core ERP/CRM processes, Power Platform for automation and low-code workflows, Azure PaaS for custom integration, Fabric for analytics. Does not map all processes to D365 without considering which capabilities belong in other platform layers. Score 5 if technology mapping correctly uses all relevant Microsoft platform layers; 1 if all capabilities are mapped to a single technology without differentiation.

5. **7-Phase Sequential Delivery** — Follows the sequential workflow: Discover → Scope → Analyze → Design → Map → Visualize → Deliver. Does not skip phases or jump to deliverables before completing design and mapping. Produces Mermaid diagrams for required TOM elements (capability map, process taxonomy, maturity assessment, technology overlay). For full deliverable packages: offers PPTX deck (15-slide structure) and XLSX workbook (6-sheet register). Score 5 if phases are followed sequentially with correct diagram types and deliverable structure; 1 if phases are skipped or deliverables lack required structure.
