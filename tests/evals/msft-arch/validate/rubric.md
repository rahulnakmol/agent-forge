Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Council of Three Parallel Dispatch** — Dispatches three independent auditor subagents simultaneously (Completeness, Consistency, Adversarial) without serializing the dispatches. Each auditor receives the full list of artifact paths and operates independently without knowledge of others' outputs. Does not run auditors sequentially or merge their prompts into a single review. Score 5 if three parallel independent auditors are dispatched for the correct artifact set; 1 if audits are serialized or merged into a single audit.

2. **Completeness Auditor Scope** — The Completeness auditor asserts every required design section exists (Overview, Architecture, Detailed Design, NFRs, Implementation Tasks, Test Strategy, Assumptions & Risks) and verifies NFR traceability from requirements through to design.md §4. Flags missing sections as blockers. Score 5 if completeness audit covers all required sections with NFR traceability; 1 if completeness audit is superficial or NFR traceability is not checked.

3. **Consistency Auditor Scope** — The Consistency auditor checks cross-artifact alignment: ADRs match requirements decisions, C4 diagrams match stack-select, component names are identical across design.md and all diagrams, API contracts match sequence diagrams, data models match schemas, handoff block integrity verified. Flags contradictions as blockers or majors. Score 5 if consistency audit catches cross-artifact conflicts including component naming; 1 if consistency check is limited to a single artifact.

4. **Adversarial Auditor Scope** — The Adversarial auditor applies a skeptical-CTO lens covering: failure modes at 10x scale, STRIDE-A security gaps, cost model validation, operability concerns, data governance, vendor lock-in risk, and day-2 operations. Produces actionable questions not just critique. Score 5 if adversarial review covers all six dimensions (scale, security, cost, operability, data governance, vendor lock-in); 1 if adversarial review is superficial or limited to a single dimension.

5. **Synthesis Protocol and Report Structure** — Synthesizes all findings using the protocol: deduplicates same root-cause findings across auditors, handles conflicting findings by keeping both with conflict tag, assigns ownership to the skill that produced the artifact, sorts by severity (blocker → major → minor → nit), and writes the final report to `validate/output.md`. Status is PASSED only when blockers = 0 AND majors = 0. Score 5 if synthesis is complete with correct severity ordering, deduplication, and PASSED/FAILED determination; 1 if findings are not synthesized or severity ordering is missing.
