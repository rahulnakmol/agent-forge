Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Parent-First Creation Order** — Creates issues in the correct order: first all parent-level items (epics, then features) capturing returned issue numbers, then child items (stories, technical stories, risks, impediments, CI items), then establishes sub-issue hierarchy. Never creates child issues before parent issues. Score 5 if parent-first ordering is correctly specified with issue number capture; 1 if ordering is arbitrary or hierarchy establishment is omitted.

2. **Duplicate Cross-Reference** — Before creating any issues, calls `list_issues` to retrieve open issues and compares backlog titles using fuzzy matching to detect duplicates. Asks the user for each potential duplicate: skip, update existing, or create new. Does not blindly create issues without checking for duplicates. Score 5 if duplicate detection is performed before creation with user decision prompts; 1 if issues are created without duplicate checking.

3. **Label and Issue Type Mapping** — Correctly maps: work item type → label (epic, feature, user-story, risk) AND issue type (if available); priority (1/2/3) → label (priority:must, priority:should, priority:could); story points → label (points:N) or body metadata. Calls `list_issue_types` to verify available types before mapping. Score 5 if all three mapping dimensions are addressed with correct field targets; 1 if only title and body are mapped without labels or issue types.

4. **Sub-Issue Hierarchy Establishment** — Uses `sub_issue_write` to link child issues to their parent using the captured issue numbers from parent creation. Verifies hierarchy with `search_issues` after creation. Does not rely on description-only references to establish parent-child relationships. Score 5 if sub_issue_write is correctly used to establish hierarchy; 1 if parent-child relationships are only mentioned in descriptions without structural linking.

5. **Summary Artifact Update** — After creation, updates `{project}/specs/backlog/backlog-summary.md` with: PRD reference, generation date, repository name, item counts by type, story point totals, and issue numbers with URLs for all created items. Provides a creation count verification against expected count. Score 5 if summary artifact is updated with all required fields including GitHub URLs; 1 if no summary update is performed.
