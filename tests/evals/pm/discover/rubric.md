Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Mode Detection and Entry Classification** — Correctly identifies Mode A (SaaS PM: building/enhancing a product, JTBD focus, handoff to epic-decompose) vs Mode B (Consulting PM: process decomposition, RACI focus, handoff to tom-architect). Applies the correct analysis frame for each mode. Does not mix Mode A persona-journey analysis with Mode B process-RACI analysis. Score 5 if mode is correctly detected and the appropriate analysis frame is applied; 1 if mode is ignored or wrong frame is applied.

2. **Solution-First Reframing** — When the user presents a solution instead of a problem statement ("We need to build X"), reframes the conversation to uncover the underlying business problem ("What problem does X solve?"). Does not accept "We need to build X" as a problem statement and proceed with discovery. Score 5 if solution-first framing is correctly challenged and reframed to problem statement; 1 if the solution is accepted as the problem statement without challenge.

3. **Initiative Classification with Decision Tree** — Classifies the initiative into one of four types using the decision tree: Product Development, Process Improvement, Process Automation, or AI/Agent-Based Automation. Applies the tree correctly: if capability exists → automation path; if cognitive judgment required → AI/Agent-Based. Validates classification with user before proceeding. Score 5 if correct classification is derived via decision tree with user validation; 1 if classification is skipped or the decision tree is not applied.

4. **Root Cause Analysis Application** — Applies Five Whys or Fishbone to distinguish root causes from symptoms. Does not accept the first-stated problem as the root cause without analysis. Produces at least 3 levels of "why" for the stated problem. For Mode B, maps current-state processes at L1-L2. Score 5 if root cause analysis method is explicitly applied producing deeper causes than symptoms; 1 if the stated problem is accepted at face value without causal analysis.

5. **Batched Questioning Discipline** — Uses batched questions (2-3 at a time) across the 5 dimensions: Business Context, Stakeholder Landscape, Problem Definition, Constraints & Dependencies, Success Criteria. Does not ask all questions at once or single questions one at a time indefinitely. Stops after 4 rounds maximum and synthesizes. Score 5 if questions are batched in 2-3 per round across all 5 dimensions; 1 if all questions are dumped at once or questioning loops without synthesis.
