Score the output 1-5 on each criterion. Return the AVERAGE.

1. **All 9 Checks Executed** — Runs all 9 structural checks without short-circuiting on first failure: (1) all 12 sections present, (2) every story has named persona, (3) every story has 3-8 Given-When-Then ACs, (4) every story has priority and complexity, (5) every feature has Star Level 1-11, (6) at least 3 success metrics with baseline/target/method/frequency, (7) at least 1 risk with full detail, (8) no open questions without owners, (9) scope in and scope out both populated. Score 5 if all 9 checks are run and reported; 1 if validation stops at first failure or fewer than 9 checks are described.

2. **Read-Only Filter Discipline** — Never modifies, rewrites, or fixes the PRD. Only reports what passes and what fails. When issues are found, suggests the user fix them in `prd-draft` and re-validate. Does not auto-correct acceptance criteria, personas, or any other content. Score 5 if read-only behavior is consistently maintained; 1 if the validator attempts to fix or rewrite PRD content.

3. **Verdict Logic Application** — Applies correct verdict logic: all 9 checks pass → PASS; 1-2 non-critical failures → PASS WITH WARNINGS; any critical failure (checks 1, 2, 3, or 4) → FAIL. Critical failures (missing sections, wrong persona format, insufficient ACs, missing priority/complexity) always produce FAIL, not PASS WITH WARNINGS. Score 5 if verdict matches the combination of check results with correct critical vs non-critical classification; 1 if verdict does not follow the defined logic.

4. **Specific Issue Reporting** — For each failed check, reports the specific issue with enough detail for the author to fix it (e.g., "Story US-3 uses 'As a user' instead of naming a persona from Section 2"). Does not report vague failures like "AC issues found." Score 5 if every failure is reported with specific, actionable detail; 1 if failures are reported vaguely without specifics.

5. **Validation Report Output** — Writes the validation report to `{project}/specs/prd/{epic-name}-validation.md` (not inline only). After writing, presents a summary to the user. For FAIL verdicts, explicitly suggests fixing issues in `prd-draft` and re-running validation. Score 5 if report file and summary with next-step guidance are both produced; 1 if output is only inline without a written report file.
