Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Platform-Agnostic Technology Mapping** — Maps TOM capabilities to the correct enterprise platform from any vendor: SAP for ERP, Salesforce for CRM/Sales, Workday for HR/Finance, ServiceNow for ITSM, Oracle for ERP/Finance, Microsoft for productivity/analytics. Does not default all capabilities to a single vendor or Microsoft-only. Recognizes the pm plugin serves multi-platform environments. Score 5 if capabilities are mapped to the correct platform for the stated technology environment; 1 if all capabilities are mapped to a single vendor without considering the stated platform context.

2. **Sequential Phase Enforcement** — Enforces the 7-phase sequential workflow (Discover → Scope → Analyze → Design → Map → Visualize → Deliver). Refuses to skip phases — specifically will not jump to technology mapping (Phase 5) without completing process decomposition (Phase 3) and design (Phase 4). Explains why the sequence matters. Score 5 if phase skipping is refused with clear rationale; 1 if technology mapping proceeds without requiring upstream phases to complete first.

3. **AI Augmentation Classification Accuracy** — Correctly classifies processes using the 5-category framework: Autonomous (full automation, no human), Human-in-the-Loop (AI executes, human approves), Copilot Assist (human executes with AI help), RPA (rule-based, no AI), Human Only (judgment required). Purchase Order approval → Human-in-the-Loop; Invoice matching (rule-based) → RPA or Autonomous; Strategic negotiation → Human Only. Score 5 if all three sample processes are correctly classified; 1 if classifications are misapplied (e.g., complex judgment classified as Autonomous).

4. **Maturity Gap Priority Ordering** — Orders epics by maturity gap size: Level 1 (Initial) gaps are higher priority than Level 2 (Developing) gaps because the starting point is further from target. Derives transformation priorities directly from the maturity assessment, not from stakeholder preference alone. Score 5 if epic ordering is correctly derived from maturity gap size; 1 if priorities are assigned without reference to maturity gaps.

5. **Deliverable Structure Completeness** — Full deliverable package includes: Mermaid diagrams (capability map, process taxonomy, maturity assessment, technology overlay), 15-slide PPTX deck (slides 1-15 as specified), and 6-sheet XLSX workbook (Capability Register, Maturity Assessment, KPI Framework, Technology Mapping, RACI Matrix, AI Augmentation). Score 5 if all three deliverable types with correct internal structure are described; 1 if deliverables are incomplete or missing required sheets/slides.
