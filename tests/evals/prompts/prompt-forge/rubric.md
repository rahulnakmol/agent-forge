Score the output 1-5 on each criterion. Return the AVERAGE.

1. **Automatic Technique Selection with Explanation** — Selects the correct prompt technique automatically based on signal detection without asking the user to choose. For analytical/ambiguous tasks: Chain-of-Thought or Tree of Thought; for classification with examples: Few-Shot; for multi-step agentic tasks: Prompt Chaining or ReAct. Explains the selection in one sentence and offers to change it. Does not present a menu of techniques for the user to pick from. Score 5 if technique is auto-selected with correct signal rationale and explanation; 1 if user is asked to choose a technique or selection rationale is missing.

2. **Artifact + Humanize + Voice Integration** — For document/spreadsheet/presentation/Markdown artifacts: activates Phase 4 (voice selection) and includes instructions to invoke the `humanize` skill with the chosen voice profile in the final prompt. For non-artifact tasks (Query/Answer, Code, Workflow): skips Phase 4 entirely. This rule is non-negotiable for artifacts. Score 5 if artifact detection correctly triggers voice selection and humanize integration; 1 if document artifacts lack humanize instructions or non-artifacts incorrectly trigger voice selection.

3. **Hardening Patterns Applied Automatically** — Applies relevant hardening patterns automatically based on signal detection, not user instruction: Anchor (always), Constraint Stack (2+ hard constraints), Confidence Gate (factual accuracy required), Failure Injection (generic-prone tasks), Assumption Audit (analytical tasks). Applies maximum 4 patterns (excluding Anchor). Tells the user which patterns are applied and why. Score 5 if hardening patterns are auto-applied with correct signal matching and cap enforcement; 1 if hardening is omitted or all patterns are applied without signal detection.

4. **Structured 5-Phase Pipeline** — Follows the pipeline phases in order: Intent Discovery (Phase 1) → Context & Constraints (Phase 2, batched 2-3 questions) → Technique Selection (Phase 3, automatic) → Voice/Artifact (Phase 4, conditional) → Assembly (Phase 5). Does not skip phases or reorder them. Assembly follows the specified order: Role/context → Anchor → Objective → Hardening patterns → Technique structure → Artifact/humanize instructions. Score 5 if all phases are executed in correct order with batched questions and proper assembly; 1 if phases are skipped, reordered, or questions are unbatched.

5. **Prompt Quality Standards** — The assembled final prompt is: self-contained (no implicit context required), includes success criteria, specifies output format explicitly, applies the selected technique pattern, includes hardening patterns, and is specific enough to produce consistent results. Ends with an offer to execute, refine, or save. Score 5 if final prompt meets all quality standards and offers post-assembly options; 1 if prompt is vague, lacks output format specification, or missing technique structure.
